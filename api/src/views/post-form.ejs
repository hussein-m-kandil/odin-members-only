<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('partials/meta', { title }) %>
  </head>
  <body>
    <%- include('partials/navbar') %>
    <div class="container mt-4" style="max-width: 480px">
      <%- include('partials/alert') %>
      <h2 class="mb-4"><%= title %></h2>
      <form method="post">
        <div class="form-floating mb-3">
          <% const titleErr = locals.validationErrors?.post_title %>
          <input
            type="text"
            class="form-control<%- titleErr ? ' is-invalid' : '' %>"
            name="post_title"
            id="post_title"
            autocomplete="on"
            placeholder="Enter Title"
            value="<%= locals.formData ? formData.post_title : '' %>"
            autofocus
          />
          <label for="post_title">Enter Title</label>
          <% if (titleErr) { %>
          <div class="invalid-feedback"><%= titleErr.msg %></div>
          <% } %>
        </div>
        <div class="form-floating mb-3">
          <% const bodyErr = locals.validationErrors?.post_body %>
          <textarea
            class="form-control<%- bodyErr ? ' is-invalid' : '' %>"
            name="post_body"
            id="post_body"
            placeholder="Enter Body"
            style="min-height: 10rem"
          >
<%= locals.formData?.post_body %></textarea
          >
          <label for="post_body">Enter Body</label>
          <% if (bodyErr) { %>
          <div class="invalid-feedback"><%= bodyErr.msg %></div>
          <% } %>
        </div>
        <div class="text-end">
          <button type="submit" class="btn btn-outline-dark btn-lg">
            <%= title %>
          </button>
        </div>
      </form>
    </div>
  </body>
</html>
